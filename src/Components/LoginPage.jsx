import Login from "@/Components/Login.jsx";

export default function LoginPage() {
    return(
        <div className={"flex items-center justify-center min-h-screen bg-gradient-to-r from-yellow-200 to-green-500"}>
            <Login/>
        </div>
    )
}